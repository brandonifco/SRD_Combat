namespace SRDCombat.Content;

/// <summary>
/// Anchors reflection at this assembly. Exists so the skeleton has something
/// real to reference before any behaviour is written; safe to delete once
/// <c>SRDCombat.Content</c> exports real types.
/// </summary>
public static class AssemblyMarker;
